import { Outlet } from "react-router-dom";

const BasicComponents = () => {
  return (
    <div>
      <h1>Basic Components, Props and Styling</h1>
      <Outlet />
    </div>
  );
};

export default BasicComponents;
